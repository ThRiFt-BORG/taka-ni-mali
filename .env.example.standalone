# CE4HOW Taka ni Mali v2 - Standalone Environment Variables
# Copy this file to .env.local and fill in your values
# This version uses JWT-based authentication without Manus dependencies

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL Connection String (recommended for production)
# Format: postgresql://username:password@host:port/database
# Example: postgresql://waste_user:waste_password@localhost:5432/waste_me_db
#DATABASE_URL=postgresql://waste_user:thrift@localhost:5585/waste_me_db

# Alternative: MySQL Connection String
# Format: mysql://username:password@host:port/database
# Example: mysql://waste_user:waste_password@localhost:3306/waste_me_db
# CORRECT PORT
DATABASE_URL=mysql://waste_user:waste_password@127.0.0.1:5595/waste_me_db
# ============================================================================
# JWT AUTHENTICATION CONFIGURATION
# ============================================================================

# JWT Secret for signing tokens (minimum 32 characters, use a strong random string)
# Generate with: openssl rand -base64 32
# Example: abc123def456ghi789jkl012mno345pqr
JWT_SECRET=change_this_to_a_long_random_string_minimum_32_characters

# JWT Algorithm (HS256 recommended)
JWT_ALGORITHM=HS256

# JWT Token Expiration (in hours)
JWT_EXPIRES_HOURS=24

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================

# Application Title (displayed in header and browser tab)
VITE_APP_TITLE=CE4HOW Taka ni Mali

# Application Logo URL (optional)
VITE_APP_LOGO=https://example.com/logo.png

# Frontend Port (Vite dev server)
VITE_PORT=5173

# ============================================================================
# BACKEND CONFIGURATION
# ============================================================================

# Backend Port
PORT=3000

# Node Environment
NODE_ENV=development

# ============================================================================
# CORS CONFIGURATION
# ============================================================================

# Allowed Origins for CORS (comma-separated)
# Must include your frontend URL and localhost for development
# Format: http://localhost:5173,https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================================================
# OPTIONAL FEATURES
# ============================================================================

# Email Configuration (for notifications - optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# File Storage (S3 or similar - optional)
# STORAGE_BUCKET=waste-me-db-uploads
# STORAGE_REGION=us-east-1
# STORAGE_ACCESS_KEY=your_access_key
# STORAGE_SECRET_KEY=your_secret_key

# Analytics (optional)
# ANALYTICS_ENDPOINT=https://analytics.example.com/api
# ANALYTICS_WEBSITE_ID=your_website_id

# ============================================================================
# NOTES
# ============================================================================

# 1. JWT Secret:
#    - MUST be at least 32 characters long
#    - Use a cryptographically secure random string
#    - Keep this secret and never commit to version control
#    - Change this in production to a unique value

# 2. Database:
#    - Ensure your database server is running
#    - Create the database before running migrations
#    - User must have CREATE TABLE and ALTER TABLE permissions
#    - PostgreSQL is recommended for production

# 3. CORS Origins:
#    - Add your production domain when deploying
#    - Format: https://yourdomain.com (no trailing slash)
#    - Separate multiple origins with commas

# 4. Email Configuration:
#    - Optional for system notifications
#    - Leave blank to disable email features
#    - For Gmail, use an App Password (not your regular password)

# 5. File Storage:
#    - Optional for file uploads
#    - Requires S3-compatible storage service
#    - Leave blank to disable file uploads

# ============================================================================
# DEMO USERS (after running seed script)
# ============================================================================

# Admin User:
#   Email: admin@example.com
#   Password: password123
#   Role: admin

# Collector 1:
#   Email: collector1@example.com
#   Password: password123
#   Role: collector

# Collector 2:
#   Email: collector2@example.com
#   Password: password123
#   Role: collector

# Public User:
#   Email: user@example.com
#   Password: password123
#   Role: user

# ============================================================================

